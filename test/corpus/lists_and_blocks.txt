==================
List item with checkbox: TODO
==================
- [ ] todo item

---

(document
  (zeroth_section
    (list
      (list_item
        (bullet)
        (checkbox)
        (paragraph)))))

==================
List item with counter
==================
1. [@5] starts at five

---

(document
  (zeroth_section
    (list
      (list_item
        (bullet)
        (counter)
        (paragraph)))))

==================
List item with counter and checkbox
==================
1. [@5] [ ] both present

---

(document
  (zeroth_section
    (list
      (list_item
        (bullet)
        (counter)
        (checkbox)
        (paragraph)))))

==================
List item with alphabetic counter
==================
1. [@b] alpha counter

---

(document
  (zeroth_section
    (list
      (list_item
        (bullet)
        (counter)
        (paragraph)))))

==================
List item with start: counter
==================
1. [@start:5] start form

---

(document
  (zeroth_section
    (list
      (list_item
        (bullet)
        (counter)
        (paragraph)))))

==================
Description list item with tag
==================
- term :: definition

---

(document
  (zeroth_section
    (list
      (list_item
        (bullet)
        (item_tag)
        (paragraph)))))

==================
Description tag splits at first separator
==================
- a :: b :: c

---

(document
  (zeroth_section
    (list
      (list_item
        (bullet)
        (item_tag)
        (paragraph)))))

==================
Description tag with multi-word term
==================
- the long term :: its definition

---

(document
  (zeroth_section
    (list
      (list_item
        (bullet)
        (item_tag)
        (paragraph)))))

==================
Colon pair without surrounding space is not a tag
==================
- foo::bar is content

---

(document
  (zeroth_section
    (list
      (list_item
        (bullet)
        (paragraph)))))

==================
Indented affiliated keyword inside a list item degrades to paragraph text (Emacs: a real keyword sibling, but list_item here has no slot for one - clean degrade, not a structural match)
==================
- box
    #+TBLFM: $2=$1

---

(document
  (zeroth_section
    (list
      (list_item
        (bullet)
        (paragraph)))))

==================
Truncated #+begin_s inside a list item degrades to paragraph text
==================
- item
  #+begin_s

---

(document
  (zeroth_section
    (list
      (list_item
        (bullet)
        (paragraph)))))

==================
A colon-prefixed non-drawer/property/fixed-width line inside a list item stays paragraph text, not just its leading colon
==================
- item
 :#+begin_s

---

(document
  (zeroth_section
    (list
      (list_item
        (bullet)
        (paragraph)))))

==================
Description tag after a checkbox
==================
- [ ] todo :: tagged

---

(document
  (zeroth_section
    (list
      (list_item
        (bullet)
        (checkbox)
        (item_tag)
        (paragraph)))))

==================
Item content starting with a bullet character is text
==================
- + plus content

---

(document
  (zeroth_section
    (list
      (list_item
        (bullet)
        (paragraph)))))

==================
Numbered description term
==================
- 5 apples :: count

---

(document
  (zeroth_section
    (list
      (list_item
        (bullet)
        (item_tag)
        (paragraph)))))

==================
Description term starting with a metacharacter
==================
- * starred :: def

---

(document
  (zeroth_section
    (list
      (list_item
        (bullet)
        (item_tag)
        (paragraph)))))

==================
Description definition starting with a metacharacter
==================
- term :: | piped

---

(document
  (zeroth_section
    (list
      (list_item
        (bullet)
        (item_tag)
        (paragraph)))))

==================
Description item with empty definition
==================
- term ::

---

(document
  (zeroth_section
    (list
      (list_item
        (bullet)
        (item_tag)))))

==================
Description definition on a continuation line
==================
- term ::
  the definition

---

(document
  (zeroth_section
    (list
      (list_item
        (bullet)
        (item_tag)
        (paragraph)))))

==================
List item with checkbox: DONE uppercase
==================
- [X] done item

---

(document
  (zeroth_section
    (list
      (list_item
        (bullet)
        (checkbox)
        (paragraph)))))

==================
List item with checkbox: DONE lowercase
==================
- [x] done item

---

(document
  (zeroth_section
    (list
      (list_item
        (bullet)
        (checkbox)
        (paragraph)))))

==================
List item with checkbox: HALF
==================
- [-] half item

---

(document
  (zeroth_section
    (list
      (list_item
        (bullet)
        (checkbox)
        (paragraph)))))

==================
Numbered list with checkbox
==================
1. [ ] numbered todo
2. [X] numbered done

---

(document
  (zeroth_section
    (list
      (list_item
        (bullet)
        (checkbox)
        (paragraph))
      (list_item
        (bullet)
        (checkbox)
        (paragraph)))))

==================
src_block with language
==================
#+begin_src lua
print("hi")
#+end_src

---

(document
  (zeroth_section
    (src_block
      (src_block_language))))

==================
src_block with language + header args
==================
#+begin_src python :tangle x.py :results output
x = 1
#+end_src

---

(document
  (zeroth_section
    (src_block
      (src_block_language)
      (block_header_args))))

==================
src_block with language, switches, and header args
==================
#+begin_src lua -n 20 :results output
x
#+end_src

---

(document
  (zeroth_section
    (src_block
      (src_block_language)
      (block_switches)
      (block_header_args))))

==================
src_block with bare switches
==================
#+begin_src emacs-lisp -r -l "fmt"
x
#+end_src

---

(document
  (zeroth_section
    (src_block
      (src_block_language)
      (block_switches))))

==================
src_block switch with colon inside a quoted value does not split header args
==================
#+begin_src lua -l "a:b" :results output
x
#+end_src

---

(document
  (zeroth_section
    (src_block
      (src_block_language)
      (block_switches)
      (block_header_args))))

==================
src_block with plus-prefixed switch
==================
#+begin_src lua +n 20
x
#+end_src

---

(document
  (zeroth_section
    (src_block
      (src_block_language)
      (block_switches))))

==================
example_block with switches
==================
#+begin_example -n
x
#+end_example

---

(document
  (zeroth_section
    (example_block
      (block_switches))))

==================
src_block without language
==================
#+begin_src
no language
#+end_src

---

(document
  (zeroth_section
    (src_block)))

==================
src_block with a blank line in its body stays inside the block
==================
#+begin_src

body after a blank line
#+end_src

---

(document
  (zeroth_section
    (src_block)))

==================
Latex environment with a blank line in its body stays inside the environment
==================
\begin{align}

x = 1
\end{align}

---

(document
  (zeroth_section
    (latex_environment
      (latexenv_name))))

==================
A footnote-def-shaped line inside a src_block body stays opaque body text
==================
#+begin_src
foo
[fn:1] a footnote
#+end_src

---

(document
  (zeroth_section
    (src_block)))

==================
A footnote-def-shaped line inside a latex environment body stays opaque body text
==================
\begin{align}
[fn:1] a footnote
\end{align}

---

(document
  (zeroth_section
    (latex_environment
      (latexenv_name))))

==================
example_block
==================
#+begin_example
plain text
#+end_example

---

(document
  (zeroth_section
    (example_block)))

==================
export_block with format
==================
#+begin_export html
<p>foo</p>
#+end_export

---

(document
  (zeroth_section
    (export_block
      (export_format))))

==================
Deeply indented bullet stays a list item
==================
                                                               - deep

---

(document
  (zeroth_section
    (list
      (list_item
        (bullet)
        (paragraph)))))

==================
Single blank line keeps following indented text in the item
==================
- foo

  bar

---

(document
  (zeroth_section
    (list
      (list_item
        (bullet)
        (paragraph)
        (paragraph)))))

==================
Two consecutive blank lines terminate the list
==================
- foo


bar

---

(document
  (zeroth_section
    (list
      (list_item
        (bullet)
        (paragraph)))
      (paragraph)))

==================
Single blank line between items keeps one list
==================
- a

- b

---

(document
  (zeroth_section
    (list
      (list_item
        (bullet)
        (paragraph))
      (list_item
        (bullet)
        (paragraph)))))

==================
Indented src block
==================
  #+begin_src lua
  print(1)
  #+end_src

---

(document
  (zeroth_section
    (src_block
      (src_block_language))))

==================
Indented table
==================
  | a | b |

---

(document
  (zeroth_section
    (table
      (table_row
        (table_cell)
        (table_cell)))))

==================
Indented comment line
==================
  # note

---

(document
  (zeroth_section
    (comment
      (comment_line
        (comment_body)))))

==================
Deeper-indented src block inside a list item stays paragraph text
==================
- item
  #+begin_src lua
  print(1)
  #+end_src

---

(document
  (zeroth_section
    (list
      (list_item
        (bullet)
        (paragraph)))))

==================
Greater block with name and args
==================
#+begin_quote foo bar
body
#+end_quote

---

(document
  (zeroth_section
    (greater_block
      (block_name)
      (block_args)
      (paragraph))))

==================
Special block
==================
#+begin_foo bar
x
#+end_foo

---

(document
  (zeroth_section
    (greater_block
      (block_name)
      (block_args)
      (paragraph))))

==================
Dynamic block with name and params
==================
#+begin: dyn :params x
body
#+end:

---

(document
  (zeroth_section
    (dynamic_block
      (block_name)
      (block_args)
      (paragraph))))

==================
Dynamic block with no args
==================
#+begin: name
body
#+end:

---

(document
  (zeroth_section
    (dynamic_block
      (block_name)
      (paragraph))))

==================
Bare #+begin: with no name is a keyword, not a dynamic block (Emacs: org-dblock-start-re requires a name)
==================
#+begin:

---

(document
  (zeroth_section
    (keyword
      (directive_name))))

==================
#+begin: followed by only trailing whitespace is still a nameless keyword
==================
#+begin: 

---

(document
  (zeroth_section
    (keyword
      (directive_name))))

==================
Nameless #+begin: keyword followed by content and #+end: (Emacs: keyword, paragraph, no dynamic_block)
==================
#+begin:
content
#+end:

---

(document
  (zeroth_section
    (keyword
      (directive_name))
    (paragraph)))

==================
Latex environment exposes its name
==================
\begin{align}
x = 1
\end{align}

---

(document
  (zeroth_section
    (latex_environment
      (latexenv_name))))

==================
Latex environment with trailing text on the same line as the opening brace (Emacs: no end-of-line anchor after \begin{NAME}, the whole line is still part of the environment)
==================
\begin{align}x = 1
\end{align}

---

(document
  (zeroth_section
    (latex_environment
      (latexenv_name))))

==================
Indented greater block
==================
  #+begin_quote
  body
  #+end_quote

---

(document
  (zeroth_section
    (greater_block
      (block_name)
      (paragraph))))

==================
Block name followed immediately by a non-name byte is a greater block, not a src block
==================
#+begin_src.
foo

---

(document
  (zeroth_section
    (greater_block
      (block_name)
      (paragraph))))

==================
Block name with a dot parses as a closed greater block
==================
#+begin_src.
foo
#+end_src.

---

(document
  (zeroth_section
    (greater_block
      (block_name)
      (paragraph))))

==================
Table cell content starting with a star stays a table row
==================
| col1 | col2 |
|------+------|
| a    |* b    |
| c    | d    |

---

(document
  (zeroth_section
    (table
      (table_header_row
        (table_cell)
        (table_cell))
      (table_rule)
      (table_row
        (table_cell)
        (table_cell))
      (table_row
        (table_cell)
        (table_cell)))))

==================
Dash-pipe line after a table row is a paragraph
==================
| a | b |
-| c | d |

---

(document
  (zeroth_section
    (table
      (table_row
        (table_cell)
        (table_cell)))
    (paragraph)))

==================
Src language absorbs trailing non-name bytes
==================
#+begin_src org]
body
#+end_src

---

(document
  (zeroth_section
    (src_block
      (src_block_language))))

==================
Unparseable switch tail becomes header args
==================
#+begin_src lua -n-20 :results output
x
#+end_src

---

(document
  (zeroth_section
    (src_block
      (src_block_language)
      (block_switches)
      (block_header_args))))

==================
Export format followed by junk has no format, whole tail is header args
==================
#+begin_export html <b>
x
#+end_export

---

(document
  (zeroth_section
    (export_block
      (block_header_args))))

==================
Dynamic block without a space before the name
==================
#+begin:myblock :param 1
content
#+end:

---

(document
  (zeroth_section
    (dynamic_block
      (block_name)
      (block_args)
      (paragraph))))

==================
A hash-prefixed non-comment/non-block-open line stays paragraph text, not just its leading hash
==================
*************** T1
#************** ENDx

---

(document
  (zeroth_section
    (inlinetask
      (inlinetask_line
        (stars)
        (title))
      (paragraph))))

==================
A drawer-shaped colon line's shape check failing degrades to paragraph text, not a truncated one
==================
* Real
:MYDRAWER:
content
#* foo
after

---

(document
  (headline
    (headline_line
      (stars)
      (title))
    (section
      (drawer
        (drawer_name)
        (paragraph)))))

==================
An unmatched #+end_ with trailing content on the same line stays paragraph text past the block-name prefix
==================
#+end_example        #+TBLFM: $2=$1

---

(document
  (zeroth_section
    (paragraph)))

==================
An unmatched #+end_ alone on its line still starts its own token (Headline-terminates-src-block precedent, no trailing content to strand)
==================
#+end_example

---

(document
  (zeroth_section
    (paragraph)))

==================
A planning-shaped line inside a latex environment stays opaque body text
==================
\begin{align}
SCHEDULED:* garbage not a timestamp

---

(document
  (zeroth_section
    (latex_environment
      (latexenv_name))))

==================
A lesser block's close line with trailing #+TBLFM: content is recognized as a formula sibling, not absorbed or ERRORed (Emacs: the whole thing is one paragraph instead - trailing content on a close line invalidates the close entirely; kept here as a divergence, matching the already-accepted "unclosed container runs to EOF" precedent, since Emacs never treats a container as a real structural element without a clean close line either)
==================
#+begin_example
body
#+end_example  #+TBLFM: $1=1

---

(document
  (zeroth_section
    (example_block)
    (formula
      (directive_name)
      (directive_value))))

==================
An indented src block's close line with trailing #+CAPTION: content is recognized as an affiliated-keyword sibling
==================
  #+begin_src sh
  code
  #+end_src  #+CAPTION: cap

---

(document
  (zeroth_section
    (src_block
      (src_block_language))
    (affiliated_keyword
      (directive_name)
      (directive_value))))

==================
A lesser block's close line with trailing content shaped like another block open starts that new (unclosed, runs-to-EOF) block
==================
#+begin_example
body
#+end_example  #+begin_src

---

(document
  (zeroth_section
    (example_block)
    (src_block)))

==================
A lesser block's close line with trailing table-row content stays plain text, not an ERROR (table rows require column 0; mid-line here they have no table slot)
==================
#+begin_src sh
code
#+end_src  | a | b |

---

(document
  (zeroth_section
    (src_block
      (src_block_language))
    (paragraph)))

==================
A verse block's close line with trailing #+TBLFM: content is recognized as a formula sibling
==================
#+begin_verse
x
#+end_verse  #+TBLFM: $1=1

---

(document
  (zeroth_section
    (verse_block)
    (formula
      (directive_name)
      (directive_value))))
