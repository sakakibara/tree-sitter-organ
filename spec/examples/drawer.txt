# Drawer: `:NAME:` ... `:END:` block under a headline.

+~~~
* Heading
:LOGBOOK:
- State "DONE" from "TODO" [2026-05-05]
:END:
~~~

+~~~
* Heading
:NOTES:
some text
:END:
~~~

# Single ":NAME:" without END: a headline or EOF terminates an
# unclosed drawer, so this is a drawer truncated at EOF.
+~~~
* Heading
:NOTES:
just text
~~~

# A zeroth-section drawer parses the same as one under a headline -
# the grammar has never scoped drawers to headline sections; a closed
# `:LOGBOOK:` / `:END:` pair here already parsed as `drawer` before
# this fix (verified against HEAD).  This case simply lacked a
# trailing newline, so it errored instead of recognising the same
# shape - the EOF-terminator fix corrects that inconsistency rather
# than introducing new behavior.
+ :LOGBOOK:

# EOF-truncated drawer-open line (no trailing newline) still parses.
+ :d:
