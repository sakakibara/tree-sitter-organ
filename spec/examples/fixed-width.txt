# Fixed-width: `: value` at BOL.

+ : literal example
+ :    indented literal

# Plain text.
- not a fixed width
# Drawer / property line.
- :NAME:
# Headline.
- * not fixed width

# EOF-truncated fixed-width line (no trailing newline) still parses.
+ : f
